export class Category {
    public category: string;
    public category_items: string;

    constructor(category: string, category_items: string) {
        this.category = category;
        this.category_items = category_items
    }
}